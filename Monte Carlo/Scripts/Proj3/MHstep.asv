function tstar = MHstep(t_matr, rho, fstat, prevPdf)
    t_prev = t_matr(end, :)
    [tstar, pdf] = rwp(t_prev, rho)
    
    %Does this step have to be logged?
    alpha = min(1, fstat(tstar)*prevPdf/(fstat(t_pre)))
    
end